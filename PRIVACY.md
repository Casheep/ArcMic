# ArcMic 隐私说明

ArcMic 的设计目标是让麦克风音频留在本机。

## ArcMic 会做什么

- 通过 Windows 共享录音接口读取实时电平。
- 通过本机 Audio Processing Object 对麦克风流进行带宽净噪、50 Hz 电流声抑制、可选 RNNoise AI 降噪和增益。
- 在用户主动点击“耳机监听”后，将处理后的麦克风声音临时播放到默认输出设备，并在 15 秒后自动停止。
- 在 `%LOCALAPPDATA%\ArcMic\settings.json` 保存开关、增益数值和本机音频端点 GUID。
- 首次配置时在 `%PROGRAMDATA%\ArcMic` 保存运行库和用于安全恢复系统音频绑定的本地备份。

## ArcMic 不会做什么

- 不录制或保存麦克风音频。
- 不上传音频、设备列表、诊断信息或使用数据。
- 不包含账号、遥测、广告、分析 SDK 或自动更新请求。
- 不读取文档、浏览器数据、密码、游戏文件或无关个人信息。
- 不注入游戏进程，不修改游戏文件。

仅当首次系统绑定失败时，程序会在 `%LOCALAPPDATA%\ArcMic\setup-error.txt` 写入本地错误堆栈。该文件不会发送到任何地方，可随时手动删除。

## 权限

首次运行或选择尚未绑定的新麦克风时会出现 Windows UAC。管理员权限仅用于：注册音频处理组件、备份和更新相应麦克风端点的 APO 设置，以及重启 Windows Audio 服务。日常增益、降噪开关和监听均在普通用户权限下完成。
